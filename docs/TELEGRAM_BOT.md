# 📱 TELEGRAM BOT — Полная документация

## Обзор

Telegram бот `@t_fomo_bot` — единая точка доставки алертов для платформы FOMO.

**Ключевой принцип:** Платформа управляет ботом, бот только принимает сообщения.

---

## 1. Типы алертов

Бот доставляет **два типа алертов**:

### 📊 Connections (Influencer Alerts)

Алерты о динамике влияния Twitter-аккаунтов:

| Тип | Emoji | Описание |
|-----|-------|----------|
| EARLY_BREAKOUT | 🚀 | Ранний рост влияния |
| STRONG_ACCELERATION | 📈 | Резкое ускорение |
| TREND_REVERSAL | 🔄 | Изменение тренда |

### 🐦 Twitter / Parser Alerts

Алерты о статусе парсера и сессий:

| Тип | Описание |
|-----|----------|
| Session OK | Сессия работает нормально |
| Session Stale | Сессия устарела |
| Session Invalid | Сессия недействительна |
| Parse Completed | Парсинг завершён |
| Parse Aborted | Парсинг прерван |

---

## 2. Команды бота

### Общие

```
/start          - Подписка на алерты + приветствие
/alerts         - Единое меню управления алертами ⚙️
/status         - Статус подключения
/help           - Справка по командам
/disconnect     - Отключить ВСЕ алерты
```

### Connections

```
/connections        - Статус Connections алертов
/connections on     - Включить
/connections off    - Выключить (mute)
```

### Twitter

```
/twitter        - Статус Twitter алертов
/twitter on     - Включить
/twitter off    - Выключить (mute)
```

---

## 3. Логика подписки

### При `/start`:

1. Создаётся/обновляется запись в `telegram_connections`
2. `isActive = true`
3. `connectionsPreferences.enabled = true` (по умолчанию)
4. `eventPreferences` = default values
5. Пользователь получает ВСЕ алерты

### Отключение:

| Команда | Что отключает |
|---------|--------------|
| `/connections off` | Только Connections алерты |
| `/twitter off` | Только Twitter алерты |
| `/disconnect` | ВСЁ (isActive=false) |

### Обратное включение:

| Команда | Что включает |
|---------|-------------|
| `/connections on` | Connections алерты |
| `/twitter on` | Twitter алерты |
| `/start` | Всё заново |

---

## 4. Модель данных

### TelegramConnectionModel

```typescript
{
  userId: string,              // ID пользователя или "tg_<chatId>"
  chatId: string,              // Telegram chat ID
  username: string,            // @username
  firstName: string,
  isActive: boolean,           // Глобальный флаг
  connectedAt: Date,
  
  // Connections (Phase 2.3)
  connectionsPreferences: {
    enabled: boolean,          // true = получает алерты
    earlyBreakout: boolean,    // Future: per-type
    strongAcceleration: boolean,
    trendReversal: boolean
  },
  
  // Twitter/Parser (existing)
  eventPreferences: {
    sessionOk: boolean,
    sessionStale: boolean,
    sessionInvalid: boolean,
    parseCompleted: boolean,
    parseAborted: boolean,
    cooldown: boolean,
    highRisk: boolean
  }
}
```

---

## 5. Admin Control

### Где настраивать

**Admin → Connections → Telegram** (`/admin/connections?tab=telegram`)

### Настройки

| Параметр | Описание | Default |
|----------|----------|---------|
| `enabled` | Глобальное вкл/выкл | false |
| `preview_only` | Логировать без отправки | true |
| `chat_id` | Admin канал (опционально) | "" |
| `type_enabled.*` | Вкл/выкл по типам | all true |
| `cooldown_hours.*` | Cooldown по типам | 12-24h |

### Actions

- **Send Test Message** — отправить тест всем подписчикам
- **Dispatch Pending** — отправить накопленные алерты

---

## 6. Flow доставки

```
┌─────────────────────────────────────────────────────────────┐
│  1. Alerts Engine создаёт alert (status: PREVIEW)           │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  2. Dispatcher проверяет Admin settings:                    │
│     - enabled? preview_only? type_enabled?                  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  3. Dispatcher проверяет cooldown per account               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  4. Dispatcher получает подписчиков:                        │
│     - isActive = true                                       │
│     - connectionsPreferences.enabled ≠ false                │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  5. Отправка через Telegram API                             │
│     - Всем подписчикам                                      │
│     - + Admin channel (если задан)                          │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│  6. Запись в delivery history                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 7. Cooldown

| Alert Type | Cooldown | Описание |
|------------|----------|----------|
| EARLY_BREAKOUT | 24h | Один аккаунт раз в сутки |
| STRONG_ACCELERATION | 12h | Один аккаунт раз в 12 часов |
| TREND_REVERSAL | 12h | Один аккаунт раз в 12 часов |
| TEST | 0 | Без ограничений |

**Важно:** Cooldown per account, не per user.

---

## 8. Environment

```bash
# backend/.env

TELEGRAM_BOT_TOKEN=8262803410:AAEO_SSg4VYEr0wb6rZfkPZm34qB-oKaoIk
PUBLIC_BASE_URL=https://svetlana-connect.preview.emergentagent.com
```

---

## 9. API Endpoints

### Admin API

```bash
# Settings
GET  /api/admin/connections/telegram/settings
PATCH /api/admin/connections/telegram/settings

# Actions
POST /api/admin/connections/telegram/test
POST /api/admin/connections/telegram/dispatch

# Analytics
GET  /api/admin/connections/telegram/history
GET  /api/admin/connections/telegram/stats
```

---

## 10. Troubleshooting

### Алерты не приходят

1. Проверить `enabled = true` в Admin
2. Проверить `preview_only = false`
3. Проверить `/connections` в боте (должен быть ON)
4. Проверить cooldown (не прошёл ли слишком мало времени)

### Bot не отвечает на команды

1. Проверить что telegram-polling запущен:
   ```bash
   tail -f /var/log/supervisor/backend-node.out.log | grep "TG Polling"
   ```
2. Проверить token:
   ```bash
   curl "https://api.telegram.org/bot$TOKEN/getMe"
   ```

### Test Message не отправляется

1. Проверить что есть подписчики:
   ```bash
   db.telegram_connections.countDocuments({ isActive: true })
   ```
2. Проверить `enabled` и `preview_only` в settings

---

## 11. Будущие улучшения

### Запланировано

- [ ] Web UI для подписки (колокольчики)
- [ ] Per-type подписка на вебе
- [ ] Inline buttons в Telegram
- [ ] Персональный cooldown

### Не планируется

- ❌ Настройки внутри бота
- ❌ Сложная логика в боте
- ❌ Отдельный бот для каждого модуля

---

*Документация: 2026-02-06*
*Bot: @t_fomo_bot*
*Version: 1.0.0*
